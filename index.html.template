<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>
    <style>
    /* Include CSS here. Minify if possible. */
    body {
        font-family: Arial, sans-serif;
    }
    .center {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        flex-direction: column;
    }
    </style>
</head>
<body>
    <div class="center">
        <h1>{{ header }}</h1>
        <p>{{ main_content }}</p>
        <div>
            <h2>Standard Plan</h2>
            <p>Get started for just $30 a year.</p>
            <a href="{{ standard_purchase_link }}">{{ standard_purchase_text }}</a>
        </div>
        <div>
            <h2>Premium Plan</h2>
            <p>Get started for just $70 a year.</p>
            <a href="{{ premium_purchase_link }}">{{ premium_purchase_text }}</a>
        </div>
    </div>
</body>
</html>
