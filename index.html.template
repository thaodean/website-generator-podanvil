<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>
</head>
<body>
    <!-- Some HTML here -->

    <div id="paypal-button-container-{{ standard_plan_id }}"></div>
    <script>
        paypal.Buttons({
            createSubscription: function(data, actions) {
                return actions.subscription.create({
                    'plan_id': '{{ standard_plan_id }}'  // Replace with your plan ID
                });
            },
            onApprove: function(data, actions) {
                alert('You have successfully subscribed to ' + data.subscriptionID);
            }
        }).render('#paypal-button-container-{{ standard_plan_id }}');  // Renders the PayPal button. Replace with your plan ID
    </script>
    
    <!-- Some HTML here -->
    
    <div id="paypal-button-container-{{ premium_plan_id }}"></div>
    <script>
        paypal.Buttons({
            createSubscription: function(data, actions) {
                return actions.subscription.create({
                    'plan_id': '{{ premium_plan_id }}'  // Replace with your plan ID
                });
            },
            onApprove: function(data, actions) {
                alert('You have successfully subscribed to ' + data.subscriptionID);
            }
        }).render('#paypal-button-container-{{ premium_plan_id }}');  // Renders the PayPal button. Replace with your plan ID
    </script>
    
    <!-- Some HTML here -->
</body>
</html>
